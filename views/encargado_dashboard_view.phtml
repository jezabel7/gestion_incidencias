<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>UAGRM - Mis Tareas</title>
    <style>
        :root { 
            --uagrm-blue: #004a87; 
            --uagrm-red: #da291c; 
            --success: #27ae60; 
        }

        body { font-family: 'Segoe UI', sans-serif; background: #f0f2f5; margin: 0; display: flex; }
        
        .sidebar { width: 250px; background: var(--uagrm-blue); height: 100vh; color: white; padding: 20px; position: fixed; box-sizing: border-box; }
        .sidebar h2 { font-size: 1.2rem; border-bottom: 1px solid rgba(255,255,255,0.2); padding-bottom: 15px; margin-top:0;}
        .main-content { margin-left: 250px; padding: 60px; width: calc(100% - 250px); box-sizing: border-box; }
        .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px; height: 45px; }
        .card-table { background: white; border-radius: 10px; box-shadow: 0 4px 6px rgba(0,0,0,0.05); overflow: hidden; }
        
        table { width: 100%; border-collapse: collapse; }
        th { background: #f8f9fa; padding: 15px; text-align: left; color: #666; font-size: 0.85rem; text-transform: uppercase; }
        td { padding: 15px; border-top: 1px solid #eee; font-size: 0.9rem; }
        
        .badge { padding: 5px 12px; border-radius: 20px; font-size: 0.75rem; font-weight: bold; color: white; }
        .status-2 { background-color: #3498db; } .status-3 { background-color: #e67e22; } .status-4 { background-color: #27ae60; }
        .form-select { padding: 5px; border-radius: 4px; border: 1px solid #ddd; font-size: 0.8rem; }
    </style>
</head>
<body>
    <div class="sidebar">
        <h2>UAGRM Gestión</h2>
        <p><small>Bienvenido, <?= $_SESSION['nombre'] ?></small></p>
        <nav>
            <a href="#" class="nav-link active" style="color:white; text-decoration:none; display:block; padding:10px; background:rgba(255,255,255,0.1); border-radius:5px; border-left:4px solid var(--uagrm-red);">🛠️ Mis Tareas</a>
            <a href="index.php?c=usuarios&a=logout" style="color:#ff7675; text-decoration:none; display:block; margin-top:50px; padding:10px;">🔴 Cerrar Sesión</a>
        </nav>
    </div>

    <div class="main-content">
        <div class="header">
            <h1 style="margin:0; letter-spacing:-1px;"><?= $titulo_panel ?></h1>
        </div>

        <div class="card-table">
            <table>
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Incidencia</th>
                        <th>Lugar</th>
                        <th>Estado Actual</th>
                        <th>Acción</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach($reportes as $r): ?>
                    <tr>
                        <td>#<?= $r['id_reportaje'] ?></td>
                        <td><strong><?= $r['titulo'] ?></strong></td>
                        <td><?= $r['nombre_lugar'] ?></td>
                        <td><span class="badge status-<?= $r['id_estado'] ?>"><?= $r['estado_nombre'] ?></span></td>
                        <td>
                            <form action="index.php?c=admin&a=actualizar_estado_daño" method="POST" style="display:flex; gap:5px;">
                                <input type="hidden" name="id_reporte" value="<?= $r['id_reportaje'] ?>">
                                <select name="nuevo_estado" class="form-select" onchange="this.form.submit()">
                                    <option value="2" <?= ($r['id_estado']==2)?'selected':'' ?>>Asignado</option>
                                    <option value="3" <?= ($r['id_estado']==3)?'selected':'' ?>>En Proceso</option>
                                    <option value="4" <?= ($r['id_estado']==4)?'selected':'' ?>>Resuelto</option>
                                </select>
                            </form>
                        </td>
                    </tr>
                    <?php endforeach; ?>
                    <?php if(empty($reportes)): ?>
                        <tr><td colspan="5" style="text-align:center; padding:50px;">No tienes tareas pendientes. 🙌</td></tr>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>