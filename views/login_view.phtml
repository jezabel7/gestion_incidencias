<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>UAGRM - Acceso Administrativo</title>
    <style>
        :root { --uagrm-blue: #004a87; --uagrm-red: #da291c; }
        body { font-family: 'Segoe UI', sans-serif; background: #f4f7f6; display: flex; align-items: center; justify-content: center; height: 100vh; margin: 0; }
        .login-card { background: white; padding: 40px; border-radius: 12px; box-shadow: 0 10px 25px rgba(0,0,0,0.1); width: 100%; max-width: 400px; border-top: 6px solid var(--uagrm-red); }
        h2 { color: var(--uagrm-blue); text-align: center; margin-bottom: 25px; }
        .form-group { margin-bottom: 20px; }
        label { display: block; margin-bottom: 8px; font-weight: bold; color: #555; }
        input { width: 100%; padding: 12px; border: 1px solid #ddd; border-radius: 6px; box-sizing: border-box; }
        .btn-login { background: var(--uagrm-blue); color: white; border: none; padding: 14px; width: 100%; border-radius: 6px; cursor: pointer; font-weight: bold; font-size: 16px; }
        .error { color: var(--uagrm-red); text-align: center; margin-bottom: 15px; font-size: 14px; }
        .back { text-align: center; margin-top: 20px; }
        .back a { color: #888; text-decoration: none; font-size: 13px; }
    </style>
</head>
<body>
    <div class="login-card">
        <h2>🔒 PANEL ADMINISTRATIVO</h2>
        <?php if(isset($error)) echo "<p class='error'>$error</p>"; ?>
        <form action="index.php?c=usuarios&a=ingresar" method="POST">
            <div class="form-group">
                <label>Carnet de Identidad:</label>
                <input type="text" name="ci" required autofocus placeholder="Ingresa tu CI">
            </div>
            <div class="form-group">
                <label>Contraseña:</label>
                <input type="password" name="password" required placeholder="••••••••">
            </div>
            <button type="submit" class="btn-login">ENTRAR AL SISTEMA</button>
        </form>
        <div class="back"><a href="index.php">← Volver al inicio público</a></div>
    </div>
</body>
</html>